{
  "name": "@telnyx/sip",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ./dist && mkdir ./dist && tsc",
    "release": "release-it"
  },
  "dependencies": {
    "eventemitter3": "^5.0.1",
    "sip.js": "^0.21.2"
  },
  "devDependencies": {
    "typescript": "^5.4.2"
  }
}
